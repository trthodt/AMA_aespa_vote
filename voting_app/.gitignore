# Các thư mục chứa thư viện (Rất nặng, không bao giờ đưa lên git)
node_modules/
jspm_packages/

# Thư mục build (Chrome sẽ load từ đây, nhưng code nguồn mới là cái cần lưu)
dist/
build/

# Các file log lỗi
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# File môi trường (Chứa API key, mật khẩu... nếu có)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# File của hệ điều hành
.DS_Store
Thumbs.db

# File cấu hình của trình soạn thảo (VS Code, WebStorm)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?